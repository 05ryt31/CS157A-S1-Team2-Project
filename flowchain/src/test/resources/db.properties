# Test-only DB config — shadows src/main/resources/db.properties on the test
# classpath. H2 in-memory keeps tests hermetic (no MySQL needed in CI).
db.url=jdbc:h2:mem:flowchain_test;MODE=MySQL;DB_CLOSE_DELAY=-1
db.user=sa
db.password=test
